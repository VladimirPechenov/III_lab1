# Отчёт по лабораторной работе № 1

**Тема:** Реализация архитектуры NPC на основе FSM и Behavior Tree

**Дисциплина:** Игровой искусственный интеллект

**Студент:** Печенов В. В.

**Преподаватель:** Колесников Н.С.

**Дата:** 23.09.2026

**Вариант:** Unity 6 (6000.3.9f1), C#, 3D, NavMesh

**Видео:** [Тут](https://youtu.be/o8LhFsdvJ4c)

## 1. Цель работы

Реализовать и сравнить два способа управления NPC-стражником: конечный автомат (FSM) и дерево поведения (Behavior Tree). Проверить патрулирование, обнаружение игрока с учётом препятствий, задержку перед преследованием, атаку и поиск потерянной цели.

## 2. Постановка задачи

Стражник проходит четыре точки патруля в комнате 20 × 20 м с четырьмя препятствиями. При появлении игрока в конусе зрения он ждёт 1 секунду, затем преследует. На расстоянии не более 1,5 м выполняет демонстрационную атаку. Если цель не видна 2 секунды, ищет её вокруг последней известной позиции. Поиск прекращается через 10 секунд. Алгоритм переключается в компоненте `GuardMode` в инспекторе.

## 3. Реализация

`Perception` проверяет дальность 10 м, угол обзора 90° и луч до игрока. Слои игрока и стражника исключены из маски препятствий, поэтому их коллайдеры не перекрывают луч. `GuardMotor` выполняет движение через `NavMeshAgent`: переход между waypoints, погоню, обход последней известной позиции и остановку для атаки. Состояния FSM реализованы явным `switch` в `GuardFSM`. Дерево поведения в `GuardBT` состоит из `Selector`, `Sequence`, `Condition` и `Action`; порядок ветвей задаёт приоритет действий. Время тревоги, потери и поиска задаётся через инспектор.

Атака выводит сообщение в Console с интервалом 1 секунда. Урон и здоровье игрока не входят в постановку задачи. Игрок перемещается клавишами WASD. Демонстрационная сцена уже включена в проект; её можно пересоздать пунктом меню **Lab 01 → Create demonstration scene**. Генератор также запекает NavMesh и добавляет сцену в Build Settings.

Диаграммы FSM и BT: [diagrams.md](diagrams.md).

## 4. Демонстрация

Скриншот сцены: ![Демонстрационная сцена с игроком, стражником и препятствиями](img/lab01-scene.png)

Ссылка на видео: [[видео]](https://youtu.be/o8LhFsdvJ4c).

## 5. Проверки и результаты

| Сценарий | Ожидаемый результат | Статус |
|---|---|---|
| Патруль | движение по четырём точкам | движение агента наблюдалось в Play Mode; полный круг не проверен |
| Зрение | цель за препятствием не обнаружена | требуется запуск в Unity |
| Тревога | переход к Chase через 1 с видимости | требуется запуск в Unity |
| Атака | сообщение в Console в пределах 1,5 м | требуется запуск в Unity |
| Потеря и поиск | Search после 2 с, возврат через 10 с | требуется запуск в Unity |
| Переключение алгоритма | в Play Mode активен выбранный компонент FSM либо BT | проверено через Unity MCP для обоих режимов |
| Узлы BT | `Selector` и `Sequence` соблюдают приоритет | 2 EditMode-теста пройдены |

Сцена создана и сохранена в Unity 6000.3.9f1. В Play Mode агент движется по запечённому NavMesh. После исправления порядка включения агента в Console не осталось ошибок и предупреждений. EditMode Test Runner: 2 пройдено, 0 провалено. Полную последовательность игровых состояний, проверку укрытия за препятствием и запись видео ещё нужно провести вручную; непроверенные сценарии выше не отмечены как пройденные.

## 6. Выводы

FSM удобно читать по явным состояниям и переходам. BT выражает приоритет действий последовательным обходом ветвей. Общие сенсор и управление агентом позволяют сравнивать архитектуры в одинаковых условиях сцены.

## 7. Источники

1. [Шаблон лабораторной работы № 1](https://github.com/kolesnikov-vstu-gameai/lab01-fsm-bt).
2. [Unity Manual: Navigation and Pathfinding](https://docs.unity3d.com/Manual/Navigation.html).
3. [Unity Scripting API: Physics.Raycast](https://docs.unity3d.com/ScriptReference/Physics.Raycast.html).

## Приложение

Личный репозиторий: [VladimirPechenov/III_lab1](https://github.com/VladimirPechenov/III_lab1). Отчёт следует перенести в официальный шаблон кафедры и экспортировать в PDF.
